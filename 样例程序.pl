const m=7, n=85;
var x,y,z,q,r,t<10>;

procedure multiply;
    var a,b;
    begin
        a:=x; b:=y; z:=0;
        for r:=5 downto r>2 do
        begin
            z:=z+1000;
            write(z);
        end
    end;
begin
    
    q:=m;
    q := q+1;
    x:=m; y:=n;
    call multiply;
    write(t[1]);
    t[1]:=1000;
    write(t[1]);
    q-=q;
    q++;
    write(q);
    q--;
    ++q;
    --q;
    write(q);
    write(z);
end.


